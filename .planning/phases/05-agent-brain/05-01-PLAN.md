---
phase: 05-agent-brain
plan: 05-01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/schemas/agent.py
  - src/services/agent_brain_service.py
  - src/services/skill_service.py
autonomous: true
requirements:
  - F9-01
  - F9-02
  - F9-03
  - F9-04
  - F9-05
must_haves:
  truths:
    - "Agent schemas define all brain types (Soul, Memory, UserProfile, Skill, Session)"
    - "AgentBrainService provides CRUD for SOUL.md, MEMORY.md, USER.md"
    - "SkillService provides skill manifest loading and invocation"
    - "pydanticai dependency added to project"
  artifacts:
    - path: "src/schemas/agent.py"
      provides: "All agent brain schemas"
      min_lines: 200
    - path: "src/services/agent_brain_service.py"
      provides: "Brain file CRUD operations"
      min_lines: 150
    - path: "src/services/skill_service.py"
      provides: "Skill manifest and invocation"
      min_lines: 150
  key_links:
    - from: "src/services/agent_brain_service.py"
      to: "GitService"
      via: "self.git_service"
      pattern: "git_service\\.(add|commit)"
    - from: "src/services/skill_service.py"
      to: "workspace/agent-brain/skills"
      via: "SKILLS_ROOT path"
      pattern: "skill_dir.*manifest\\.yaml"
---

<objective>
Wave 1: Foundation — pydanticai dependency, agent schemas, AgentBrainService, SkillService.

Purpose: Establish the core data models and services for the Agent Brain.
Output: Schemas, services, dependency added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-agent-brain/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Add pydanticai dependency</name>
  <files>pyproject.toml</files>
  <action>
Add `"pydanticai>=0.0.0"` to dependencies section of `pyproject.toml`. Use the `uv add pydanticai` pattern to ensure proper formatting.

Verify the dependency section includes:
- `"pydanticai"` in the main dependencies array
- The `[project.optional-dependencies]` section exists with dev dependencies
  </action>
  <verify>
    <automated>grep -l "pydanticai" pyproject.toml && ruff check pyproject.toml</automated>
  </verify>
  <done>pyproject.toml contains pydanticai in dependencies</done>
</task>

<task type="auto">
  <name>Task 1.2: Create agent schemas</name>
  <files>src/schemas/agent.py</files>
  <action>
Create `src/schemas/agent.py` with all Pydantic models for agent brain:

Soul schemas (F9-02): SoulSection, SoulUpdate, SoulResponse
Memory schemas (F9-03): MemorySection, MemoryUpdate, MemoryResponse
User profile schemas (F9-04): UserProfileSection, UserProfileUpdate, UserProfileResponse
Skill schemas (F9-05): SkillTrigger, SkillInputsSchema, SkillOutputsSchema, SkillManifest, SkillResponse, SkillListResponse, SkillInvokeRequest, SkillInvokeResponse
Session schemas (F9-06): SessionSummary, SessionResponse, SessionListResponse

Use `pydantic.BaseModel` with `model_config = ConfigDict(extra="forbid")` where appropriate.
Import `datetime` from datetime, `Any` from typing, `Field` and `BaseModel` from pydantic.
  </action>
  <verify>
    <automated>ruff check src/schemas/agent.py && python -c "from src.schemas.agent import SoulSection, SoulUpdate, SoulResponse, MemorySection, MemoryUpdate, MemoryResponse, UserProfileSection, UserProfileUpdate, UserProfileResponse, SkillManifest, SkillResponse, SkillListResponse, SkillInvokeRequest, SkillInvokeResponse, SessionSummary, SessionResponse, SessionListResponse; print('All schemas import OK')"</automated>
  </verify>
  <done>src/schemas/agent.py exists with all classes, passes ruff check, all schemas importable</done>
</task>

<task type="auto">
  <name>Task 1.3: Create AgentBrainService</name>
  <files>src/services/agent_brain_service.py</files>
  <action>
Create `src/services/agent_brain_service.py` with `AgentBrainService` class.

Brain root: `BRAIN_ROOT = Path("workspace/agent-brain")` per D-66.

Methods required:
- `_parse_soul_section(content: str) -> SoulSection` — parse sections from SOUL.md content
- `_extract_section(content: str, heading: str) -> str | None` — extract section content
- `_extract_list_section(content: str, heading: str) -> list[str]` — extract bullet list sections
- `read_soul() -> SoulResponse` — async read SOUL.md
- `update_soul(update: SoulUpdate) -> SoulResponse` — async update SOUL.md with git auto-commit
- `read_memory() -> MemoryResponse` — async read MEMORY.md
- `update_memory(update: MemoryUpdate) -> MemoryResponse` — async update MEMORY.md with git auto-commit
- `read_user_profile() -> UserProfileResponse` — async read USER.md
- `update_user_profile(update: UserProfileUpdate) -> UserProfileResponse` — async update USER.md with git auto-commit
- `list_sessions() -> SessionListResponse` — list all session summaries from sessions/ directory
- `get_session(session_id: str) -> SessionResponse` — get specific session
- `write_session_summary(session_id: str, summary: SessionSummary) -> SessionResponse` — write session summary
- `validate_brain_structure() -> dict[str, bool]` — validate required dirs and files exist

Git auto-commit on mutations per D-82. Use `GitService` from `src.services.git_service`.
Use `structlog` for logging, `yaml` for manifest parsing, `re` for section extraction.
  </action>
  <verify>
    <automated>ruff check src/services/agent_brain_service.py && python -c "from src.services.agent_brain_service import AgentBrainService; print('AgentBrainService imports OK')"</automated>
  </verify>
  <done>src/services/agent_brain_service.py exists with AgentBrainService class, all methods async, passes ruff check</done>
</task>

<task type="auto">
  <name>Task 1.4: Create SkillService</name>
  <files>src/services/skill_service.py</files>
  <action>
Create `src/services/skill_service.py` with `SkillService` class.

Skills root: `SKILLS_ROOT = Path("workspace/agent-brain/skills")` per D-67.

Methods required:
- `_load_manifest(skill_dir: Path) -> SkillManifest | None` — load manifest.yaml
- `_load_body(skill_dir: Path) -> str` — load SKILL.md procedure body
- `list_skills() -> SkillListResponse` — async list all skills from skills/ directory
- `get_skill(skill_name: str) -> SkillResponse | None` — async get specific skill
- `invoke_skill(skill_name: str, input_data: dict[str, Any]) -> SkillInvokeResponse` — async invoke skill per D-68
- `create_skill(skill_name, description, procedure, triggers, inputs_schema, outputs_schema) -> SkillResponse` — async create skill with manifest.yaml and SKILL.md
- `update_skill(skill_name, ...) -> SkillResponse | None` — async update existing skill

Skill format per D-67: `skills/<skill-name>/manifest.yaml` + `SKILL.md` procedure body.
Use `structlog` for logging, `yaml` for manifest parsing, `uuid` for skill IDs.
  </action>
  <verify>
    <automated>ruff check src/services/skill_service.py && python -c "from src.services.skill_service import SkillService; print('SkillService imports OK')"</automated>
  </verify>
  <done>src/services/skill_service.py exists with SkillService class, all methods async, passes ruff check</done>
</task>

</tasks>

<verification>
- ruff check passes on: pyproject.toml, src/schemas/agent.py, src/services/agent_brain_service.py, src/services/skill_service.py
- All Python imports successful
- pydanticai dependency present in pyproject.toml
</verification>

<success_criteria>
- pyproject.toml has pydanticai dependency
- src/schemas/agent.py has all 16 schema classes (SoulSection, SoulUpdate, SoulResponse, MemorySection, MemoryUpdate, MemoryResponse, UserProfileSection, UserProfileUpdate, UserProfileResponse, SkillTrigger, SkillInputsSchema, SkillOutputsSchema, SkillManifest, SkillResponse, SkillListResponse, SkillInvokeRequest, SkillInvokeResponse, SessionSummary, SessionResponse, SessionListResponse)
- src/services/agent_brain_service.py has AgentBrainService with all brain file CRUD methods
- src/services/skill_service.py has SkillService with manifest loading and invocation
- All files pass ruff check
</success_criteria>

<output>
After completion, create `.planning/phases/05-agent-brain/{phase}-01-SUMMARY.md`
</output>
